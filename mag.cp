#include<stdio.h>
int main()
{
	int a[i],org,n,i,d;
	printf("enter a and n");
	scanf("%d %d",&a,&n);
	org=a;
	for(i=0;i<n;i++)
	{
	  d=i;
	  d=d+i;	
	}
	printf("%d",d);
	for()
	if(d==org)
	{
		printf("ma");
	}
	else
	{
		printf("not ma");
	}
}