#include<stdio.h>
#include<string.h>
int main()
{
	int n=10;
	int count,i;
	char a[i];
	printf("enter the string\n",a[i]);
	scanf("%s",&a[i]);
	for(i=0;i<n;i++)
	{
		if(a[i]<n)
		{
			count=i;
			count ++;
		}
	}	
	printf("%d",count);
	return 0;
}