#include<stdio.h>
void cm()
{
		char x[50];
	char cml[20];
	int cmd;

	printf("car name:",x);
	scanf("%s",&x);
	printf("car manf date:",cmd);
	scanf("%d",&cmd);
	printf("car model:",cml);
	scanf("%s",&cmd);

}
int main()
{
		char x[50];
	char cml[20];
	int cmd;
printf("%s",x);	
printf("%s",cmd);	
printf("%s",cml);	
	cm();
		cm();
	return 0;
}